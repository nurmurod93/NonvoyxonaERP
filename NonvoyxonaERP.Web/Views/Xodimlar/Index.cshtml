@model List<NonvoyxonaERP.Application.DTOs.Xodim.XodimGetDTO>
@{
    ViewData["Title"] = "Xodimlar";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <h2>👥 Xodimlar</h2>
    <a asp-action="Create" class="btn btn-primary">➕ Yangi Xodim</a>
</div>

<div class="card shadow-sm">
    <div class="card-body p-0">
        <table class="table table-hover table-striped mb-0">
            <thead class="table-dark">
                <tr>
                    <th>ID</th>
                    <th>FIO</th>
                    <th>Telefon</th>
                    <th>Lavozimi</th>
                    <th>Maosh turi</th>
                    <th>Asosiy maosh</th>
                    <th>Aktiv</th>
                    <th>Amallar</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Id</td>
                        <td>@item.FIO</td>
                        <td>@item.Telefon</td>
                        <td>@item.Lavozimi</td>
                        <td>@item.MaoshiTuri</td>
                        <td>@item.AsosiyMaoshi.ToString("N0")</td>
                        <td>
                            @if (item.Aktiv)
                            {
                                <span class="badge bg-success">Ha</span>
                            }
                            else
                            {
                                <span class="badge bg-danger">Yo'q</span>
                            }
                        </td>
                        <td>
                            <form asp-action="Delete" asp-route-id="@item.Id" method="post"
                                  onsubmit="return confirm('O\'chirishni tasdiqlaysizmi?')">
                                <button type="submit" class="btn btn-sm btn-danger">🗑️ O'chir</button>
                            </form>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>