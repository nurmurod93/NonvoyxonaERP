@model List<NonvoyxonaERP.Application.DTOs.Kassa.KassaGetDTO>
@{
    ViewData["Title"] = "Kassalar";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <h2>💵 Kassalar</h2>
    <a asp-action="Create" class="btn btn-primary">➕ Yangi Kassa</a>
</div>

<div class="card shadow-sm">
    <div class="card-body p-0">
        <table class="table table-hover table-striped mb-0">
            <thead class="table-dark">
                <tr>
                    <th>ID</th>
                    <th>Nomi</th>
                    <th>Turi</th>
                    <th>Qoldiq (so'm)</th>
                    <th>Aktiv</th>
                    <th>Amallar</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Id</td>
                        <td>@item.Nomi</td>
                        <td>@item.Turi</td>
                        <td>@item.Qoldiq.ToString("N0")</td>
                        <td>
                            @if (item.Aktiv)
                            {
                                <span class="badge bg-success">Ha</span>
                            }
                            else
                            {
                                <span class="badge bg-danger">Yo'q</span>
                            }
                        </td>
                        <td>
                            <form asp-action="Delete" asp-route-id="@item.Id" method="post"
                                  onsubmit="return confirm('O\'chirishni tasdiqlaysizmi?')">
                                <button type="submit" class="btn btn-sm btn-danger">🗑️ O'chir</button>
                            </form>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>